import { getUserFromSessionServer } from "@/lib/auth/server";
// TEMPORAIRE: noop pour débloquer le build
// On recâblera ensuite avec le bon helper d'auth serveur existant dans le projet.
export default async function ActiveSubBannerServer() {
  return null;
}
