import React from "react";
import PricingShowcase from "@/components/pricing/PricingShowcase";

export const metadata = {
  title: "Tarifs | Site Sciences",
  description:
    "Comparez Normal, Gold et Platine. −20% en annuel (engagement 12 mois) et Pack Famille −20% (≥ 2 niveaux).",
};

export default function PageTarifs() {
  return <PricingShowcase />;
}
